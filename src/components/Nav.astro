---
import { getRelativeLocaleUrl } from 'astro:i18n';
import { NAV_LINKS } from '../config';

interface Props {
  lang: 'en' | 'fr';
}

const { lang } = Astro.props;
const otherLocale = lang === 'fr' ? 'en' : 'fr';

// Strip locale prefix to get the bare path for the toggle URL
const rawPath    = Astro.url.pathname;
const barePath   = rawPath.startsWith('/fr') ? rawPath.slice(3) || '/' : rawPath;
const toggleUrl  = getRelativeLocaleUrl(otherLocale, barePath);

const links = NAV_LINKS[lang];
---

<header class="border-b border-mchiato-surface0">
  <nav class="max-w-3xl mx-auto px-6 py-3 flex items-center gap-6">

    <a href={getRelativeLocaleUrl(lang, '/')}
       class="text-mchiato-peach text-sm font-medium hover:opacity-80 transition-opacity">
      ~/darko
    </a>

    <div class="flex gap-5 ml-auto items-center">
      {links.map(link => (
        <a href={link.href}
           class="text-xs text-mchiato-overlay1 hover:text-mchiato-peach transition-colors">
          {link.href.startsWith('http') && '↗ '}{link.label}
        </a>
      ))}

      <!-- Language toggle -->
      <div class="flex items-center gap-1 ml-2 pl-4 border-l border-mchiato-surface0">
        {lang === 'en' ? (
          <span class="text-xs text-mchiato-peach font-medium">EN</span>
        ) : (
          <a href={toggleUrl} class="text-xs text-mchiato-overlay1 hover:text-mchiato-peach transition-colors">EN</a>
        )}
        <span class="text-mchiato-overlay0 text-xs">|</span>
        {lang === 'fr' ? (
          <span class="text-xs text-mchiato-peach font-medium">FR</span>
        ) : (
          <a href={toggleUrl} class="text-xs text-mchiato-overlay1 hover:text-mchiato-peach transition-colors">FR</a>
        )}
      </div>
    </div>

  </nav>
</header>
